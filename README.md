# bella-baruk
Sistema de MMN em php
#sql de filtar os usuário agrupados por nivel na matriz
#    SELECT id_user_matriz as 'matriz_root', level, count(level) as 'quantidade' FROM `matriz` where id_user_matriz = 3 GROUP BY level

